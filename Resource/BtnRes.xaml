<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:PhoneBook_Klimov.Pages">
    <Style TargetType="{x:Type Border}" x:Key="Grid_Item_Anim">
        <Setter Property="Background" Value="White"/>
        <Style.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <EventTrigger.Actions>
                    <BeginStoryboard>
                        <Storyboard>
                            <ColorAnimation To="#FF959595" Storyboard.TargetProperty="(Grid.Background).(SolidColorBrush.Color)"
                                            FillBehavior="HoldEnd" Duration="0:0:0:0.2"/>
                            <ColorAnimation To="White" Storyboard.TargetProperty="(Label.Foreground).(SolidColorBrush.Color)"
                                            FillBehavior="HoldEnd" Duration="0:0:0:0.2"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger.Actions>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <ColorAnimation To="{x:Null}" Storyboard.TargetProperty="(Grid.Background).(SolidColorBrush.Color)"
                                        FillBehavior="HoldEnd" Duration="0:0:0:0.2"/>
                        <ColorAnimation To="#FF1D4B7F" Storyboard.TargetProperty="(Label.Foreground).(SolidColorBrush.Color)"
                                        FillBehavior="HoldEnd" Duration="0:0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>